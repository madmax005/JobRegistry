
@{
    ViewBag.Title = "Registration Home";
}
@if (Session["UserID"] != null)
{
    <h4>Hello @Session["UserName"].ToString()</h4>
}

    <div>
       
            <a href="@Url.Action("Create", "JobSeeker")">Register</a>
       
            <a href="@Url.Action("AddJobs", "Jobs")">Add Job</a>
                       
    </div>



<a href="@Url.Action("LogOut", "Login")">LogOut</a>